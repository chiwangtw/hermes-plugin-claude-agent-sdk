[pytest]
# Lives in tests/ so the rootdir is tests/: the plugin root carries an __init__.py (Hermes loads it as
# a package) and pytest >= 8 would otherwise collect it as a Package and import it at setup, where
# its relative imports fail. Run: <hermes venv python> -m pytest tests -v
